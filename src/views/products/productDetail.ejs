<!DOCTYPE html>
<html lang="es">
    <%- include('../partials/head',{tittle:"Detalles"})%>
    <body class="body-login">
    <%- include('../partials/header') %>
    <br><br>
    <section class="detallesDelProd">
        <div class="spyFma" id ="box-product">
            <% for( let i = 0; i < product.images.length; i++ ) { %>
                <img id="box-product__imgZ" src="/images/products/<%=product.images[i].name ? product.images[i].name :  "defect.png" %> " alt="<%= product.name %> ">
            <% } %>
        </div>

        <div class="detalle-prod">
            <div class="titulo">
                <h1><%= product.name %> </h1>
                <p><%=   %> </p>
                <p>$<%= product.price %> </p>
            </div>
        
        
        <div class="estado">
            <h4 data-text="Estado: <%= product.state.type %> ...">Estado: <%= product.state.type %> ...</h4>
            <ul>
            <!-- <li> </li> -->
            </ul>
        </div>
        <% if (locals.userLoged) { %>
            <% if (locals.userLoged.roles_id ==  1) { %>
                
            <% } %>
        <% }else{ %>
                <div class="agregarCarrito">
                    <input type="submit" class="btn2" value="Agregar carrito">
                    <i class="fas fa-shopping-cart"></i>
                </div>
                <br>
            <% } %>
        <% if (locals.userLoged) { %>
            <% if (locals.userLoged.roles_id ==  1) { %>
                <div class="boton-delate">
                    <a href="/products/edit/<%= product.id %> " class="action-button-edit">EDITAR PRODUCTO</a>
                            <form action="/products/delete/<%= product.id %>" method="POST" style="display: inline-flex">
                                <button type="submit" class="action-button-delete">ELIMINAR</button>
                            </form>
                    </div>
            <% }else{ %>
                
            <% } %>
        <% } %>
        

        


        <h3>ENVÍO GRATIS:</h3>

        <h4> <i class="fas fa-share-alt"></i> COMPARTIR EN: </h4>

        <section class="redes">
        <div class="wrapper">
            <div class="botton">
                <div class="icon"><i class="fab fa-whatsapp"></i></div>
                <span>Whatsapp</span>
            </div>
            <div class="botton">
                <div class="icon"><i class="fab fa-instagram" _mstvisible="2"></i></i></div>
                <span>Instagram</span>
            </div>
            <div class="botton">
                <div class="icon"><i class="fab fa-facebook"></i></i></div>
                <span>Facebook</span>
            </div>
            <div class="botton">
                <div class="icon"><i class="fab fa-twitter"></i></div>
                <span>Twitter</span>
            </div>
        </div>
    </section>

        <article>
        
        </article>
        <span class="descrip-oculto">
            <%= product.description %> 
        </span>

        <div class="Formato-libro">
        <ul>
            <li>Formato: <%= product.size.format %> </li>
            <li>Tamaño: 13cm x 18cm</li>
            <li><%= product.detail.type %> </li>
            <li>Incluye paginas a color</li>
        </ul>
        </div>

    </div>
    </section>

    <main class="index-img">
        <h2>PRODUCTOS RELACIONADOS</h2>
        <section class="Productos-relacionados contenedor-grid">
            
            <% for( let i =0; i < products.length; i++ ) { %>
                <article>
                <a href="/products/detailProduct/<%= products[i].id %> ">
                    <% for( let j = 0; j < products[i].images.length; j++ ) { %>
                        <img src="/images/products/<%= products[i].images[j].name %> " alt="portada<%= products[i].name %> ">
                    <% } %>
                   
                </a>
                <div class="main-product">
                    
                    <h3><%= products[i].name%></i></h3>
                    <p>$<%= products[i].price %> </p>
                    <% if (locals.userLoged) { %>
                        <% if (locals.userLoged.roles_id ==  1) { %>
                            
                        <% } %>
                    <% }else{ %>
                            <div class="agregarCarrito">
                                <input type="submit" class="btn2" value="Agregar carrito">
                                <i class="fas fa-shopping-cart"></i>
                            </div>
                        <% } %>
                       
                    </form>
                </div>
            </article>
            <% } %>
            

        </section>
        
    </main>
    <script src="/js/productDetail.js"></script>

    <%- include('../partials/footer') %>

</body>
</html>